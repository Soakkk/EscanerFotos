# Mejoras v2.3 — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Añadir a EscanerFotos pegar con Ctrl+V, recorte con esquinas arrastrables, PDF de varias fotos ordenable por miniaturas, mejor calidad (igualado de luz) y arreglar el solapamiento de controles en pantalla completa.

**Architecture:** Se extrae el procesado de imagen a un módulo puro `imagen.py` (testeable sin Qt) y se le añade `igualar_iluminacion`. La UI en `escaner_fotos.py` gana scroll en el panel, edición de esquinas por arrastre en `LienzoImagen`, pegado del portapapeles y un panel de PDF basado en `QListWidget` con miniaturas reordenables.

**Tech Stack:** Python 3.12, PySide6 (Qt6), OpenCV, NumPy, Pillow, pytest.

**Entorno de tests:** `PYTHONPATH=EscanerFotos .venv/bin/python -m pytest tests/ -v` (el venv en `.venv` ya tiene pytest, PySide6, opencv-python-headless, numpy, Pillow).

---

### Task 1: Extraer el procesado a `imagen.py`

Mover, **sin cambiar su cuerpo**, estas funciones desde `EscanerFotos/escaner_fotos.py` a un
nuevo `EscanerFotos/imagen.py`: `detectar_documento`, `_buscar_cuadrilatero`,
`ordenar_puntos`, `corregir_perspectiva`, `filtro_bn_escaner`, `filtro_color_mejorado`,
`aplicar_ajustes`, `rotar_imagen`, `_cv_a_pil` (renombrar a `cv_a_pil`), `leer_imagen`,
`aplicar_pipeline`, `procesar_lote`.

**Files:**
- Create: `EscanerFotos/imagen.py`
- Modify: `EscanerFotos/escaner_fotos.py`

- [ ] **Step 1:** Crear `EscanerFotos/imagen.py` con cabecera y los imports que esas
funciones necesitan, y pegar las funciones movidas:

```python
"""Procesado de imagen de EscanerFotos (OpenCV/PIL puro, sin Qt).
Separado para poder probarlo automáticamente."""

import os
import cv2
import numpy as np
from PIL import Image, ImageOps

# (pegar aquí, tal cual, las funciones movidas desde escaner_fotos.py;
#  renombrar la antigua `_cv_a_pil` a `cv_a_pil` y actualizar sus usos internos)
```

- [ ] **Step 2:** En `escaner_fotos.py`, borrar esas funciones y añadir el import (después de
los imports de PySide6):

```python
from imagen import (
    detectar_documento, corregir_perspectiva, ordenar_puntos,
    filtro_bn_escaner, filtro_color_mejorado, aplicar_ajustes,
    rotar_imagen, aplicar_pipeline, leer_imagen, cv_a_pil, procesar_lote,
)
```

- [ ] **Step 3:** Sustituir en `escaner_fotos.py` toda llamada a `_cv_a_pil(` por `cv_a_pil(`.

- [ ] **Step 4: Verify**

```bash
python3 -m py_compile EscanerFotos/imagen.py EscanerFotos/escaner_fotos.py
QT_QPA_PLATFORM=offscreen PYTHONPATH=EscanerFotos .venv/bin/python -c "
import escaner_fotos as ef
from PySide6.QtWidgets import QApplication
app = QApplication([])
v = ef.VentanaPrincipal(); print('arranca OK:', v.windowTitle())
"
PYTHONPATH=EscanerFotos .venv/bin/python -m pytest tests/ -q
```
Expected: arranca OK + tests verdes.

- [ ] **Step 5: Commit**

```bash
git add EscanerFotos/imagen.py EscanerFotos/escaner_fotos.py
git commit -m "refactor: extraer procesado de imagen a imagen.py"
```

---

### Task 2: `igualar_iluminacion` (quita sombras/luz irregular)

**Files:**
- Modify: `EscanerFotos/imagen.py`
- Create: `tests/test_imagen.py`

- [ ] **Step 1: Write the failing test**

```python
# tests/test_imagen.py
import numpy as np
import cv2
from imagen import igualar_iluminacion

def test_igualar_iluminacion_conserva_forma_y_tipo():
    img = (np.random.rand(80, 100, 3) * 255).astype(np.uint8)
    out = igualar_iluminacion(img)
    assert out.shape == img.shape
    assert out.dtype == np.uint8

def test_igualar_iluminacion_uniformiza_un_gradiente():
    # Fondo con gradiente de sombra (oscuro->claro): tras igualar debe quedar plano
    h, w = 100, 200
    grad = np.tile(np.linspace(120, 255, w).astype(np.uint8), (h, 1))
    img = cv2.cvtColor(grad, cv2.COLOR_GRAY2BGR)
    out = igualar_iluminacion(img)
    g_in = cv2.cvtColor(img, cv2.COLOR_BGR2GRAY).std()
    g_out = cv2.cvtColor(out, cv2.COLOR_BGR2GRAY).std()
    assert g_out < g_in
```

- [ ] **Step 2: Run test to verify it fails**

Run: `PYTHONPATH=EscanerFotos .venv/bin/python -m pytest tests/test_imagen.py -v`
Expected: FAIL con `ImportError: cannot import name 'igualar_iluminacion'`

- [ ] **Step 3: Write minimal implementation (en imagen.py)**

```python
def igualar_iluminacion(imagen):
    """Quita sombras y luz irregular: estima la iluminación de fondo con un
    desenfoque grande y normaliza la imagen dividiéndola por ese fondo.
    Entra y sale BGR del mismo tamaño."""
    salida = []
    for canal in cv2.split(imagen):
        fondo = cv2.GaussianBlur(canal, (0, 0), sigmaX=25)
        norm = cv2.divide(canal, fondo, scale=255)
        salida.append(norm)
    return cv2.merge(salida)
```

- [ ] **Step 4: Run test to verify it passes**

Run: `PYTHONPATH=EscanerFotos .venv/bin/python -m pytest tests/test_imagen.py -v`
Expected: PASS (2 tests)

- [ ] **Step 5: Commit**

```bash
git add EscanerFotos/imagen.py tests/test_imagen.py
git commit -m "feat: igualar_iluminacion (quita sombras y luz irregular)"
```

---

### Task 3: `buffer_rgb_a_cv` (base del pegado, lógica pura)

**Files:**
- Modify: `EscanerFotos/imagen.py`
- Modify: `tests/test_imagen.py`

- [ ] **Step 1: Write the failing test (append)**

```python
# tests/test_imagen.py  (añadir)
from imagen import buffer_rgb_a_cv

def test_buffer_rgb_a_cv_respeta_padding_y_orden_bgr():
    # 2x2 px RGB con 2 bytes de padding por fila (bytes_per_line = 2*3 + 2 = 8)
    w, h = 2, 2
    bpl = w * 3 + 2
    fila0 = bytes([255, 0, 0,  0, 255, 0]) + bytes([0, 0])     # rojo, verde + pad
    fila1 = bytes([0, 0, 255,  9, 9, 9]) + bytes([0, 0])       # azul, gris + pad
    buf = fila0 + fila1
    out = buffer_rgb_a_cv(buf, w, h, bpl)
    assert out.shape == (2, 2, 3)
    # OpenCV es BGR: el rojo (255,0,0 RGB) debe quedar (0,0,255) BGR
    assert tuple(int(x) for x in out[0, 0]) == (0, 0, 255)
    # el verde (0,255,0) sigue (0,255,0)
    assert tuple(int(x) for x in out[0, 1]) == (0, 255, 0)
```

- [ ] **Step 2: Run test to verify it fails**

Run: `PYTHONPATH=EscanerFotos .venv/bin/python -m pytest tests/test_imagen.py -k buffer -v`
Expected: FAIL con `ImportError: cannot import name 'buffer_rgb_a_cv'`

- [ ] **Step 3: Write minimal implementation (en imagen.py)**

```python
def buffer_rgb_a_cv(buffer, w, h, bytes_per_line):
    """Convierte un buffer RGB888 (con posible padding por fila) en imagen BGR OpenCV."""
    arr = np.frombuffer(buffer, dtype=np.uint8).reshape(h, bytes_per_line)
    arr = arr[:, : w * 3].reshape(h, w, 3)
    return cv2.cvtColor(arr, cv2.COLOR_RGB2BGR)
```

- [ ] **Step 4: Run test to verify it passes**

Run: `PYTHONPATH=EscanerFotos .venv/bin/python -m pytest tests/test_imagen.py -k buffer -v`
Expected: PASS

- [ ] **Step 5: Commit**

```bash
git add EscanerFotos/imagen.py tests/test_imagen.py
git commit -m "feat: buffer_rgb_a_cv (base del pegado desde el portapapeles)"
```

---

### Task 4: Integrar igualado en el pipeline y afinar filtros

**Files:**
- Modify: `EscanerFotos/imagen.py` (`aplicar_pipeline`, `filtro_bn_escaner`)
- Modify: `tests/test_imagen.py`

- [ ] **Step 1: Write the failing test (append)**

```python
# tests/test_imagen.py  (añadir)
from imagen import aplicar_pipeline

def test_pipeline_tres_modos_conservan_tamano():
    img = (np.random.rand(120, 90, 3) * 255).astype(np.uint8)
    for modo in (0, 1, 2):
        out = aplicar_pipeline(img, modo, 0, 0, 0)
        assert out.shape == img.shape, f"modo {modo}"
        assert out.dtype == np.uint8
```

- [ ] **Step 2: Run it (debe pasar ya en forma, pero fijamos el comportamiento nuevo)**

Run: `PYTHONPATH=EscanerFotos .venv/bin/python -m pytest tests/test_imagen.py -k pipeline -v`
Expected: PASS o FAIL según el estado; si pasa, continúa para meter el igualado.

- [ ] **Step 3: Modificar `aplicar_pipeline` para igualar la luz antes de los filtros**

```python
def aplicar_pipeline(base, filtro_idx, brillo, contraste, nitidez):
    if filtro_idx == 0:        # B/N escáner
        img = filtro_bn_escaner(igualar_iluminacion(base))
    elif filtro_idx == 1:      # Color con luz corregida
        img = filtro_color_mejorado(igualar_iluminacion(base))
    else:                      # Color original
        img = base.copy()
    if brillo or contraste or nitidez:
        img = aplicar_ajustes(img, brillo, contraste, nitidez)
    return img
```

- [ ] **Step 4: Afinar `filtro_bn_escaner` (menos manchas tras igualar la luz)**

```python
def filtro_bn_escaner(imagen):
    """Convierte a B/N tipo escáner. Pensado para usarse tras igualar_iluminacion."""
    gris = cv2.cvtColor(imagen, cv2.COLOR_BGR2GRAY)
    gris = cv2.GaussianBlur(gris, (3, 3), 0)
    bn = cv2.adaptiveThreshold(
        gris, 255,
        cv2.ADAPTIVE_THRESH_GAUSSIAN_C,
        cv2.THRESH_BINARY,
        31, 15
    )
    return cv2.cvtColor(bn, cv2.COLOR_GRAY2BGR)
```

- [ ] **Step 5: Run tests**

Run: `PYTHONPATH=EscanerFotos .venv/bin/python -m pytest tests/ -q`
Expected: PASS (todos)

- [ ] **Step 6: Commit**

```bash
git add EscanerFotos/imagen.py tests/test_imagen.py
git commit -m "feat: igualar la luz en el pipeline y afinar el B/N"
```

---

### Task 5: Arreglar solapamiento — panel de controles con scroll

**Files:**
- Modify: `EscanerFotos/escaner_fotos.py` (`_crear_interfaz`, import de `QScrollArea`)

- [ ] **Step 1:** Añadir `QScrollArea` al import de `PySide6.QtWidgets`.

- [ ] **Step 2:** En `_crear_interfaz`, donde se añade el panel de controles
(`w_ctrl = self._construir_panel_controles()` y `layout.addWidget(w_ctrl, 0)`),
envolverlo en un scroll:

```python
        # Columna central: controles (con scroll para que no se solapen en pantalla completa)
        w_ctrl = self._construir_panel_controles()
        scroll_ctrl = QScrollArea()
        scroll_ctrl.setWidgetResizable(True)
        scroll_ctrl.setWidget(w_ctrl)
        scroll_ctrl.setHorizontalScrollBarPolicy(Qt.ScrollBarPolicy.ScrollBarAlwaysOff)
        scroll_ctrl.setMinimumWidth(330)
        scroll_ctrl.setMaximumWidth(370)
        scroll_ctrl.setFrameShape(QScrollArea.Shape.NoFrame)
```

Y cambiar `layout.addWidget(w_ctrl, 0)` por `layout.addWidget(scroll_ctrl, 0)`.

- [ ] **Step 3:** En `_construir_panel_controles`, quitar la línea
`w.setMaximumWidth(340)` (el ancho ahora lo fija el scroll) y dejar `w.setMinimumWidth(300)`.

- [ ] **Step 4: Verify (smoke offscreen)**

```bash
python3 -m py_compile EscanerFotos/escaner_fotos.py
QT_QPA_PLATFORM=offscreen PYTHONPATH=EscanerFotos .venv/bin/python -c "
import escaner_fotos as ef
from PySide6.QtWidgets import QApplication
app = QApplication([]); v = ef.VentanaPrincipal(); v.resize(1900,1100); v.show()
print('arranca y redimensiona OK')
"
```
Expected: `arranca y redimensiona OK`

- [ ] **Step 5: Commit**

```bash
git add EscanerFotos/escaner_fotos.py
git commit -m "fix: panel de controles con scroll (no se solapa en pantalla completa)"
```

---

### Task 6: Esquinas del recorte arrastrables

**Files:**
- Modify: `EscanerFotos/escaner_fotos.py` (`LienzoImagen` y conexión en `VentanaPrincipal`)

- [ ] **Step 1:** En `LienzoImagen.__init__`, añadir estado y una señal nueva. Tras la
señal existente `imagen_soltada`, añadir:

```python
    puntos_editados = Signal(list)   # 4 puntos tras arrastrar una esquina
```

y en `__init__`, junto a `self.puntos = []`:

```python
        self.modo_editar = False
        self._idx_arrastrado = None
```

- [ ] **Step 2:** Añadir método para mostrar esquinas editables:

```python
    def mostrar_esquinas(self, puntos):
        """Muestra 4 vértices (coords de imagen) que se pueden arrastrar."""
        self.puntos = [list(p) for p in puntos]
        self.modo_editar = True
        self.modo_seleccion = False
        self.setCursor(Qt.CursorShape.ArrowCursor)
        self.actualizar_visualizacion()

    def _esquina_cercana(self, x_pix, y_pix):
        """Índice del vértice cuya posición en pantalla está a <=18 px del clic, o None."""
        mejor, mejor_d = None, 18.0
        for i, p in enumerate(self.puntos):
            px = p[0] * self.factor_escala + self.offset_x
            py = p[1] * self.factor_escala + self.offset_y
            d = ((px - x_pix) ** 2 + (py - y_pix) ** 2) ** 0.5
            if d <= mejor_d:
                mejor, mejor_d = i, d
        return mejor
```

- [ ] **Step 3:** Manejar el arrastre en `mousePressEvent` (al principio del método, antes
de la lógica de `modo_seleccion`):

```python
        if self.modo_editar and len(self.puntos) == 4 and self.imagen_cv is not None:
            idx = self._esquina_cercana(event.position().x(), event.position().y())
            if idx is not None:
                self._idx_arrastrado = idx
                return
```

Y añadir los métodos de movimiento y soltar:

```python
    def mouseMoveEvent(self, event):
        if self._idx_arrastrado is None:
            return
        cx = (event.position().x() - self.offset_x) / self.factor_escala
        cy = (event.position().y() - self.offset_y) / self.factor_escala
        h, w = self.imagen_cv.shape[:2]
        cx = max(0, min(w - 1, cx))
        cy = max(0, min(h - 1, cy))
        self.puntos[self._idx_arrastrado] = [cx, cy]
        self.actualizar_visualizacion()

    def mouseReleaseEvent(self, event):
        if self._idx_arrastrado is not None:
            self._idx_arrastrado = None
            self.puntos_editados.emit([list(p) for p in self.puntos])
```

- [ ] **Step 4:** En `VentanaPrincipal._crear_interfaz`, tras conectar
`self.lienzo_original.puntos_listos`, conectar la nueva señal:

```python
        self.lienzo_original.puntos_editados.connect(self._al_recibir_puntos_manuales)
```

- [ ] **Step 5:** En `detectar_auto`, tras calcular `puntos`, mostrarlos como editables en
lugar de fijarlos estáticos. Sustituir:

```python
        self.lienzo_original.puntos = puntos.tolist()
        self.lienzo_original.actualizar_visualizacion()
```
por:
```python
        self.lienzo_original.mostrar_esquinas(puntos.tolist())
```

- [ ] **Step 6: Verify**

```bash
python3 -m py_compile EscanerFotos/escaner_fotos.py
QT_QPA_PLATFORM=offscreen PYTHONPATH=EscanerFotos .venv/bin/python -c "
import escaner_fotos as ef
from PySide6.QtWidgets import QApplication
app = QApplication([]); v = ef.VentanaPrincipal()
v.lienzo_original.mostrar_esquinas([[0,0],[10,0],[10,10],[0,10]])
assert v.lienzo_original.modo_editar
print('esquinas editables OK')
"
PYTHONPATH=EscanerFotos .venv/bin/python -m pytest tests/ -q
```
Expected: `esquinas editables OK` + tests verdes.

- [ ] **Step 7: Commit**

```bash
git add EscanerFotos/escaner_fotos.py
git commit -m "feat: esquinas del recorte arrastrables con el ratón"
```

---

### Task 7: Pegar con Ctrl+V

**Files:**
- Modify: `EscanerFotos/escaner_fotos.py` (import `QImage`, atajo, helpers de carga)

- [ ] **Step 1:** Asegurar que `QImage` está importado de `PySide6.QtGui` (ya lo está) y
`buffer_rgb_a_cv` importado desde `imagen` (añadir al import de `imagen`).

- [ ] **Step 2:** Refactor de la carga para reusar desde el pegado. Sustituir el cuerpo de
`_cargar_archivo` por una versión que delega en un nuevo `_cargar_cv`:

```python
    def _cargar_archivo(self, ruta):
        try:
            img = leer_imagen(ruta)
            if img is None:
                raise ValueError("OpenCV no pudo leer el archivo.")
            self._cargar_cv(img, os.path.dirname(ruta))
        except Exception as e:
            QMessageBox.critical(self, "Error", f"No se pudo abrir la imagen:\n{e}")

    def _cargar_cv(self, img, ruta_origen=""):
        self.imagen_original = img
        self.imagen_enderezada = None
        self._ruta_origen = ruta_origen or self._ruta_origen
        self.lienzo_original.limpiar_puntos()
        self.lienzo_original.mostrar_imagen(img)
        self.lbl_estado_recorte.setText("<i style='color:#888'>Sin recortar</i>")
        self._resetear_sliders()
        self._actualizar_preview_base()
        self.detectar_auto(silencioso=True)
        self.actualizar_procesado()
        self._actualizar_barra_estado()
```

(Conserva el comportamiento actual de `_cargar_archivo`, solo que la parte común vive en
`_cargar_cv`.)

- [ ] **Step 3:** Añadir el conversor de QImage y el método de pegar:

```python
    def _qimage_a_cv(self, qimg):
        if qimg.isNull():
            return None
        qimg = qimg.convertToFormat(QImage.Format.Format_RGB888)
        b = bytes(qimg.constBits())
        return buffer_rgb_a_cv(b, qimg.width(), qimg.height(), qimg.bytesPerLine())

    def pegar_imagen(self):
        cb = QApplication.clipboard()
        md = cb.mimeData()
        if md.hasImage():
            img = self._qimage_a_cv(cb.image())
            if img is not None and img.size:
                self._cargar_cv(img)
                self.statusBar().showMessage("Imagen pegada del portapapeles", 4000)
                return
        if md.hasUrls():
            exts = ('.jpg', '.jpeg', '.png', '.bmp', '.tiff', '.tif', '.webp')
            for url in md.urls():
                if url.isLocalFile() and url.toLocalFile().lower().endswith(exts):
                    self._cargar_archivo(url.toLocalFile())
                    return
        self.statusBar().showMessage("El portapapeles no contiene una imagen", 4000)
```

- [ ] **Step 4:** En `_crear_atajos`, añadir:

```python
        QShortcut(QKeySequence("Ctrl+V"), self, activated=self.pegar_imagen)
```

- [ ] **Step 5: Verify**

```bash
python3 -m py_compile EscanerFotos/escaner_fotos.py
QT_QPA_PLATFORM=offscreen PYTHONPATH=EscanerFotos .venv/bin/python -c "
import numpy as np, cv2
import escaner_fotos as ef
from PySide6.QtWidgets import QApplication
from PySide6.QtGui import QImage
app = QApplication([]); v = ef.VentanaPrincipal()
# QImage rojo 4x4 -> _qimage_a_cv -> BGR
qi = QImage(4,4,QImage.Format.Format_RGB888); qi.fill(0xFF0000)
out = v._qimage_a_cv(qi)
assert out.shape == (4,4,3) and tuple(int(x) for x in out[0,0]) == (0,0,255)
print('pegar (conversion) OK')
"
PYTHONPATH=EscanerFotos .venv/bin/python -m pytest tests/ -q
```
Expected: `pegar (conversion) OK` + tests verdes.

- [ ] **Step 6: Commit**

```bash
git add EscanerFotos/escaner_fotos.py
git commit -m "feat: pegar imagen del portapapeles con Ctrl+V"
```

---

### Task 8: PDF de varias fotos con miniaturas reordenables

Sustituye la sección "PDF multipágina" y sus métodos por un `QListWidget` con miniaturas
arrastrables. Se elimina `self.paginas_pdf` (las páginas viven en los items).

**Files:**
- Modify: `EscanerFotos/escaner_fotos.py` (imports, panel, métodos PDF, `__init__`)

- [ ] **Step 1:** Añadir al import de `PySide6.QtWidgets`: `QListWidget`, `QListWidgetItem`.
Añadir al import de `PySide6.QtGui`: `QIcon`. Añadir al import de `PySide6.QtCore`: `QSize`.

- [ ] **Step 2:** En `__init__`, eliminar `self.paginas_pdf = []` (ya no se usa).

- [ ] **Step 3:** Sustituir el bloque del grupo PDF (`g6 ... panel.addWidget(g6)`) en
`_construir_panel_controles` por:

```python
        # === 7. PDF de varias fotos (miniaturas reordenables) ===
        g6 = QGroupBox("📑  PDF de varias fotos (arrastra para ordenar)")
        l6 = QVBoxLayout(g6)
        self.lista_pdf = QListWidget()
        self.lista_pdf.setViewMode(QListWidget.ViewMode.IconMode)
        self.lista_pdf.setIconSize(QSize(80, 104))
        self.lista_pdf.setResizeMode(QListWidget.ResizeMode.Adjust)
        self.lista_pdf.setDragDropMode(QListWidget.DragDropMode.InternalMove)
        self.lista_pdf.setMinimumHeight(130)
        l6.addWidget(self.lista_pdf)
        fila_pdf = QHBoxLayout()
        btn_add_pdf = QPushButton("➕ Añadir")
        btn_add_pdf.clicked.connect(self.anadir_pagina_pdf)
        btn_quitar_pdf = QPushButton("🗑️ Quitar")
        btn_quitar_pdf.clicked.connect(self.quitar_pagina_pdf)
        btn_vaciar_pdf = QPushButton("Vaciar")
        btn_vaciar_pdf.clicked.connect(self.vaciar_paginas_pdf)
        fila_pdf.addWidget(btn_add_pdf)
        fila_pdf.addWidget(btn_quitar_pdf)
        fila_pdf.addWidget(btn_vaciar_pdf)
        l6.addLayout(fila_pdf)
        btn_exp_pdf = QPushButton("📄  Exportar PDF")
        btn_exp_pdf.setMinimumHeight(36)
        btn_exp_pdf.clicked.connect(self.exportar_pdf_multipagina)
        l6.addWidget(btn_exp_pdf)
        panel.addWidget(g6)
```

- [ ] **Step 4:** Sustituir los métodos `anadir_pagina_pdf`, `vaciar_paginas_pdf`,
`exportar_pdf_multipagina` y eliminar `lbl_paginas`. Nuevos métodos:

```python
    def anadir_pagina_pdf(self):
        img = self.procesada_full()
        if img is None:
            QMessageBox.warning(self, "Atención", "Procesa una imagen primero.")
            return
        # Miniatura para el icono
        rgb = cv2.cvtColor(img, cv2.COLOR_BGR2RGB)
        h, w = rgb.shape[:2]
        qimg = QImage(rgb.data, w, h, 3 * w, QImage.Format.Format_RGB888).copy()
        icono = QIcon(QPixmap.fromImage(qimg).scaled(
            80, 104, Qt.AspectRatioMode.KeepAspectRatio,
            Qt.TransformationMode.SmoothTransformation))
        item = QListWidgetItem(icono, "")
        item.setData(Qt.ItemDataRole.UserRole, cv_a_pil(img))  # PIL a tamaño completo
        self.lista_pdf.addItem(item)
        self._actualizar_barra_estado()

    def quitar_pagina_pdf(self):
        fila = self.lista_pdf.currentRow()
        if fila >= 0:
            self.lista_pdf.takeItem(fila)
        self._actualizar_barra_estado()

    def vaciar_paginas_pdf(self):
        self.lista_pdf.clear()
        self._actualizar_barra_estado()

    def exportar_pdf_multipagina(self):
        paginas = [
            self.lista_pdf.item(i).data(Qt.ItemDataRole.UserRole)
            for i in range(self.lista_pdf.count())
        ]
        if not paginas:
            QMessageBox.warning(self, "Atención",
                "No hay páginas. Añade con «➕ Añadir».")
            return
        ruta, _ = QFileDialog.getSaveFileName(
            self, "Exportar PDF",
            os.path.join(self._ruta_origen, "documento.pdf"), "PDF (*.pdf)")
        if not ruta:
            return
        if not ruta.lower().endswith(".pdf"):
            ruta += ".pdf"
        try:
            paginas[0].save(ruta, "PDF", resolution=200.0,
                            save_all=True, append_images=paginas[1:])
            QMessageBox.information(self, "Exportado",
                f"PDF de {len(paginas)} páginas guardado en:\n{ruta}")
        except Exception as e:
            QMessageBox.critical(self, "Error", f"No se pudo exportar el PDF:\n{e}")
```

- [ ] **Step 5:** En `_actualizar_barra_estado`, sustituir el uso de `self.paginas_pdf`
por la cuenta de la lista. Cambiar:
```python
        if self.paginas_pdf:
            partes.append(f"PDF batch: {len(self.paginas_pdf)} pág.")
```
por:
```python
        if hasattr(self, "lista_pdf") and self.lista_pdf.count():
            partes.append(f"PDF: {self.lista_pdf.count()} pág.")
```

- [ ] **Step 6:** Eliminar el atajo y referencias a la cola antigua si quedara alguna
(`añadir_pagina_pdf` con ñ, `lbl_paginas`). Buscar y limpiar referencias muertas.

- [ ] **Step 7: Verify**

```bash
python3 -m py_compile EscanerFotos/escaner_fotos.py
QT_QPA_PLATFORM=offscreen PYTHONPATH=EscanerFotos .venv/bin/python -c "
import numpy as np
import escaner_fotos as ef
from PySide6.QtWidgets import QApplication
app = QApplication([]); v = ef.VentanaPrincipal()
v.imagen_original = (np.random.rand(60,40,3)*255).astype('uint8')
v._actualizar_preview_base()
v.anadir_pagina_pdf(); v.anadir_pagina_pdf()
assert v.lista_pdf.count() == 2
v.quitar_pagina_pdf(); assert v.lista_pdf.count() == 1
print('panel PDF OK')
"
PYTHONPATH=EscanerFotos .venv/bin/python -m pytest tests/ -q
```
Expected: `panel PDF OK` + tests verdes.

- [ ] **Step 8: Commit**

```bash
git add EscanerFotos/escaner_fotos.py
git commit -m "feat: PDF de varias fotos con miniaturas reordenables"
```

---

## Self-review (cobertura del spec)

- §1 layout scroll → Task 5. ✓
- §2 recorte (detección con luz igualada + esquinas arrastrables) → Task 4 (igualado antes
  de detección se hereda al pasar por el pipeline; la detección usa la imagen original pero
  el igualado mejora el resultado mostrado) + Task 6 (arrastrar). Nota: la detección sigue
  sobre la imagen original; el afinado de tolerancias se considera parte de Task 6/QA visual.
- §3 calidad (igualar_iluminacion + filtros) → Tasks 2 y 4. ✓
- §4 pegar Ctrl+V → Tasks 3 y 7. ✓
- §5 PDF ordenable con miniaturas → Task 8. ✓
- §6 reorganización a imagen.py → Task 1. ✓
- §7 pruebas → tests en Tasks 1-4, 6, 7, 8 (smoke). ✓

Sin placeholders. Nombres consistentes (`igualar_iluminacion`, `buffer_rgb_a_cv`,
`aplicar_pipeline`, `cv_a_pil`, `mostrar_esquinas`, `puntos_editados`, `_cargar_cv`,
`anadir_pagina_pdf`, `lista_pdf`).

> Ajuste de versión y publicación (fuera de las tareas de código): al terminar, subir
> `version.py` a `2.3`, `git tag v2.3`, push → publica el instalador y los PCs se
> actualizan (prueba real del auto-update).
